services:
  - type: web
    name: youtube-audio-looper
    runtime: docker
    plan: free
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: YTDLP_COOKIES_PATH
        sync: false
      - key: YTDLP_USER_AGENT
        sync: false
